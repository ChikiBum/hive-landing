import React from 'react';
import userPublicPage from '../common/hocs/user-public-page.hoc';

const HomePage = () => {
  return <div>React is ready HOME</div>;
};

export default userPublicPage(HomePage);
